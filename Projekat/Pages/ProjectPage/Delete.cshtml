@page 
@model Projekat.Pages.ProjectPage.DeleteModel

@{
    ViewData["Title"] = "Delete Project";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Delete Project</h1>

<h3>Are you sure you want to delete this project?</h3>

<div>
    <h4>Project Details</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Project.Name</dd>

        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Model.Project.Description</dd>

        <dt class="col-sm-2">Start Date</dt>
        <dd class="col-sm-10">@Model.Project.StartDate</dd>

        <dt class="col-sm-2">End Date</dt>
        <dd class="col-sm-10">@Model.Project.EndDate</dd>

        <dt class="col-sm-2">Budget</dt>
        <dd class="col-sm-10">@Model.Project.Budget</dd>

        <dt class="col-sm-2">Status</dt>
        <dd class="col-sm-10">@Model.Project.Status</dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Project.ID" />
        <input type="submit" value="Delete" class="btn btn-danger" /> 
        <a asp-page="./Project" class="btn btn-secondary">Back to List</a>
    </form>
</div>
